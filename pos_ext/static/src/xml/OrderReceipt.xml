<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="OrderReceipt" t-inherit="point_of_sale.OrderReceipt" t-inherit-mode="extension" owl="1">
        <xpath expr="//span[@t-esc='env.pos.format_currency(receipt.total_discount)']/.." position="replace"/>
        <xpath expr="//div[hasclass('pos-receipt')]" position="attributes">
            <attribute name="style">padding-right: 25px;</attribute>
        </xpath>
        <xpath expr="//div[hasclass('pos-receipt-amount')]" position="before">
            <div class="pos-receipt-amount" style="padding-bottom: 20px">
                SUBTOTAL
                <span t-esc="env.pos.format_currency(receipt.total_without_tax)" class="pos-receipt-right-align"/>
                <br/>
            </div>
        </xpath>
        <xpath expr="//t[@t-esc='receipt.footer']" position="before">
            <div style="margin-bottom: 20px;"><t t-esc="receipt.partner.name"/></div>
            <div style="margin-bottom: 20px;">___________________________</div>
            <div style="margin-bottom: 20px; font-size: 10px;">
                All returns will be issued as store credit exclusively under the name of the customer as shown in this receipt.<br/>
                Todas las devoluciones se emitirán como crédito de la tienda exclusivamente a nombre del cliente como se muestra en este recibo.
            </div>
        </xpath>
    </t>
    <t t-name="OrderLinesReceipt" t-inherit="point_of_sale.OrderLinesReceipt" t-inherit-mode="extension" owl="1">
        <xpath expr="//t[@t-foreach='receipt.orderlines']" position="replace">
            <t t-foreach="receipt.orderlines" t-as="line" t-key="line.id">
                <div style="margin-bottom: 20px;">
                    <div t-esc="line.product_name_wrapped[0]" />
                    <WrappedProductNameLines line="line" />
                    <span class="price_display pos-receipt-right-align">
                        <t t-esc="env.pos.format_currency_no_symbol(line.price_display)" />
                    </span>
                </div>
            </t>
        </xpath>
    </t>
</templates>
