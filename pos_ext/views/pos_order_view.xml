<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_pos_order_tree" model="ir.ui.view">
        <field name="name">pos.order.tree.multicurr</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_order_tree"/>
        <field name="arch" type="xml">
            <field name="amount_total" position="replace">
                <field name="amount_currency" sum="Amount Currency"/>
            </field>
        </field>
    </record>

    <record id="view_pos_pos_form" model="ir.ui.view">
        <field name="name">pos.order.form.editable.multicurr</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_pos_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='lines']/tree/field[@name='price_subtotal']" position="after">
                <field name="amount_currency"/>
            </xpath>
            <xpath expr="//field[@name='payment_ids']/tree/field[@name='amount']" position="after">
                <field name = "amount_currency" groups="base.group_multi_currency"/>
                <field name = "currency" groups="base.group_multi_currency"/>
            </xpath>
        </field>
    </record>
</odoo>
